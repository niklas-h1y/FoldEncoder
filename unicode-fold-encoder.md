# Unicode Fold Encoder

Here is the complete source code for the standalone encoder and decoder tool:

```html
<!doctype html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Unicode Fold Encoder</title><style>*{box-sizing:border-box}body{margin:0;min-width:280px;background:#0b0d10;color:#f4f7f2;font:16px/1.5 system-ui,sans-serif}main{width:min(720px,100%);margin:auto;padding:28px 18px}h1{margin:0 0 8px;font-size:clamp(28px,8vw,44px);letter-spacing:-.05em}p{margin:0 0 24px;color:#99a59f}label{display:block;margin:18px 0 8px;color:#aab5af;font-size:12px;font-weight:700;letter-spacing:.12em;text-transform:uppercase}textarea{display:block;width:100%;min-height:180px;resize:vertical;border:1px solid #303a40;border-radius:12px;padding:14px;background:#151c20;color:#f4f7f2;font:16px/1.55 system-ui,sans-serif;outline:0}textarea:focus{border-color:#d7ff5f}#output{color:#efffae;font:24px/1.4 Georgia,serif}div{display:flex;gap:10px;margin:14px 0;flex-wrap:wrap}button{min-height:56px;flex:1;border:1px solid #d7ff5f;border-radius:11px;background:#d7ff5f;color:#10150d;font-weight:800;font-size:15px;cursor:pointer}button:active{transform:scale(.98)}#decode{border-color:#39464a;background:#192126;color:#e8f0eb}#status{min-height:24px;margin-top:12px;color:#9aa59f;font-size:13px}</style></head><body><main><h1>Unicode Fold</h1><p>Private standalone encoder. Only the input, encoding, and decoding controls are included.</p><label for="input">Input</label><textarea id="input" placeholder="Type text or compressed Unicode here..."></textarea><div><button id="encode" type="button">Encode</button><button id="decode" type="button">Decode</button></div><label for="output">Output</label><textarea id="output" readonly placeholder="Your result appears here..."></textarea><div id="status" role="status"></div></main><script>const base=0x4e00;const input=document.getElementById('input');const output=document.getElementById('output');const status=document.getElementById('status');function encode(text){const padded=text+' '.repeat((3-text.length%3)%3);let result='';for(let i=0;i<padded.length;i+=3){const a=Math.max(0,Math.min(95,padded[i].codePointAt(0)-32));const b=Math.max(0,Math.min(95,padded[i+1].codePointAt(0)-32));const c=Math.max(0,Math.min(95,padded[i+2].codePointAt(0)-32));result+=String.fromCodePoint(base+a*9216+b*96+c)}return result}function decode(text){let result='';for(let i=0;i<text.length;i++){const cp=text.codePointAt(i);if(cp>65535)i++;const folded=cp-base;if(folded<0||folded>=96*96*96)throw Error('Invalid folded symbol');const a=Math.floor(folded/9216);const b=Math.floor(folded%9216/96);const c=folded%96;result+=String.fromCodePoint(a+32)+String.fromCodePoint(b+32)+String.fromCodePoint(c+32)}return result.replace(/ +\$/,'')}document.getElementById('encode').addEventListener('click',()=>{try{output.value=encode(input.value);status.textContent='Encoded successfully.'}catch(error){status.textContent='Encoding failed.'}});document.getElementById('decode').addEventListener('click',()=>{try{output.value=decode(input.value);status.textContent='Decoded successfully.'}catch(error){status.textContent='Decoding failed: invalid folded text.'}});</script></body></html>
```
